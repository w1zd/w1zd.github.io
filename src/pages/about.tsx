import React from "react"
import { graphql, Link } from "gatsby"
import Layout from "../components/layout"

const Tags = ({ data }) => {
  return (
    <Layout>
      About Me!
    </Layout>
  )
}

export default Tags

